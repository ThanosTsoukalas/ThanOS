//Used in workspace.html
// If the user has a Desktop PC or iMac, this script also prevents the battery status 
// from being displayed on the desktop.

if (navigator.getBattery) {
    // if battery API is working
}
else {
    // if battery API is not working
}